version: 2
models:
  - name: subquery__
    columns:
      - name: graphql
        tests:
          - test_marketplace_udf:
              name: test_subquery__graphql_status_200
              args: >
                'kepler-network'
                , {'query':'query { _mmrs (first: 5) { nodes { key } } }'}

              filter: :status_code
              expected: 200